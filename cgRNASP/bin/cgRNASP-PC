#! /bin/bash
if [[ $# -ne 1 ]]; then
    echo 'Usage: cgRNASP-PC /path/to/dir'
    exit 1
fi

tmp=$(mktemp)
cd /opt/cgRNASP/cgRNASP-PC
./cgRNASP-PC $1 $(find $1 -type f -name '*.pdb' | wc -l) ${tmp} >/dev/null
cat ${tmp}
rm ${tmp}
