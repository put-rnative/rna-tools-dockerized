#! /bin/bash
if [[ $# -ne 1 ]]; then
    echo 'Usage: cgRNASP-C /path/to/dir'
    exit 1
fi

tmp=$(mktemp)
cd /opt/cgRNASP/cgRNASP-C
./cgRNASP-C $1 $(find $1 -type f -name '*.pdb' | wc -l) ${tmp} >/dev/null
cat ${tmp}
rm ${tmp}
